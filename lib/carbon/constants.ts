/**
 * Shared temporal constants for annualising usage figures.
 *
 * Centralised here so the calculator and the insights engine stay in lock-step
 * and there is a single source of truth (no duplicated magic numbers).
 */
export const WEEKS_PER_YEAR = 52;
export const MONTHS_PER_YEAR = 12;
export const DAYS_PER_YEAR = 365;
